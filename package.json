{
  "name": "wc-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "build": "npm run build --workspace=client",
    "etl": "npm run etl --workspace=server",
    "preview": "vercel dev"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "dependencies": {
    "rss-parser": "^3.13.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "vercel": "^41.7.0"
  }
}
